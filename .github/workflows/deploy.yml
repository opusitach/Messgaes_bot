name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.7.0
      with:
        ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

    - name: Configure SSH access
      run: |
        mkdir -p ~/.ssh
        # Добавляем fingerprint GitHub
        ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts
        # Добавляем fingerprint EC2 сервера
        ssh-keyscan -H 16.171.182.173 >> ~/.ssh/known_hosts
        chmod 600 ~/.ssh/known_hosts

    - name: Deploy and restart service
      run: |
        ssh -o StrictHostKeyChecking=no \
            ubuntu@16.171.182.173 \
            "cd /home/ubuntu/Messgaes_bot && \
            git config --global --add safe.directory /home/ubuntu/Messgaes_bot && \
            GIT_SSH_COMMAND='ssh -i ~/.ssh/github_key' git fetch origin main && \
            GIT_SSH_COMMAND='ssh -i ~/.ssh/github_key' git reset --hard origin/main && \
            sudo systemctl restart tg_bot"
